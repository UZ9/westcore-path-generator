import { Card } from "@material-ui/core";
import { button, useControls } from "leva";
import React from "react";

export default function UIManager() {

    // const viewModeButton = button(() => alert("thou be clicked"))

    // useControls({ 
    //     "Name": "Red_Mogo_Waypoint_1",
        
    //     "Marker Mode": button(() => alert("thou has been clicked")),
    //  })

    return (
        <div/>
    );
}